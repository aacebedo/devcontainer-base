---
name: Release

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

permissions: {}

jobs:
  release:
    uses: aacebedo/boilerplate-templates/.github/workflows/reusable-release.yaml@v0.5.0
    secrets: inherit
    permissions:
      contents: write
      packages: write
