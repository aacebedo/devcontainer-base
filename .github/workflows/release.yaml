---
name: Release

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

permissions: {}

jobs:
  release:
    uses: aacebedo/boilerplate-templates/.github/workflows/reusable-release.yaml@v0.4.4
    secrets: inherit
    permissions:
      contents: write
      packages: write
